/*
  Warnings:

  - You are about to drop the column `recurring` on the `ExpenseCategory` table. All the data in the column will be lost.

*/
-- AlterTable
ALTER TABLE `ExpenseCategory` DROP COLUMN `recurring`;
